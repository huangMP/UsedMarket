<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usedmarket.dao.UserInfoDao">

    <!--表名 -->
    <sql id="tableName">
        t_user_info
    </sql>

    <!-- 字段 -->
    <sql id="Field">
        user_id,
        username,
        password,
        phone,
        sex,
        attachment_id,
        birthday,
        ID_num,
        real_name,
        registration_date,
        school,
        shipping_address,
        constellation,
        blood_type
    </sql>

    <!-- 字段值 -->
    <sql id="FieldValue">
        #{userId},
        #{username},
        #{password},
        #{phone},
        #{sex},
        #{attachmentId},
        #{birthday},
        #{IDNum},
        #{realName},
        #{registrationDate},
        #{school},
        #{shippingAddress},
        #{constellation},
        #{bloodType}
    </sql>

    <!-- 字段别名 -->
    <sql id="FieldAlias">
        user_id "userId",
        username "username",
        password "password",
        phone "phone",
        sex "sex",attachmentId
        attachment_id "attachmentId",
        birthday "birthday",
        ID_num "IDNum",
        real_name "realName",
        registration_date "registrationDate",
        school "school",
        shipping_address "shippingAddress"
    </sql>

    <!-- 查找 -->
    <select id="findByUserId" resultType="UserInfo">
        SELECT * FROM
        <include refid="tableName"></include>
        WHERE user_id=#{userId}
    </select>

    <!-- 查找 UserInfoCustom -->
    <select id="findUserInfoCustomByUsername" resultType="UserInfoCustom">
        SELECT
        t_user_info.user_id "userId",
        t_user_info.username "username",
        t_user_info.password "password",
        t_user_info.phone "phone",
        t_user_info.sex "sex",
        t_attachment.file_path "headPortraitPath",
        t_attachment.narrow_image_path "narrowHeadPortraitPath",
        t_user_info.birthday "birthday",
        t_user_info.ID_num "IDNum",
        t_user_info.real_name "realName",
        t_user_info.registration_date "registrationDate",
        t_user_info.school "school",
        t_user_info.shipping_address "shippingAddress"
        FROM
        t_user_info
        INNER JOIN t_attachment ON t_user_info.attachment_id=t_attachment.attachment_id
        WHERE username=#{username}
    </select>

    <!-- 新增-->
    <insert id="insert" parameterType="UserInfo">
        insert into
          <include refid="tableName"></include>
        (
          <include refid="Field"></include>
        ) values (
          <include refid="FieldValue"></include>
        )
    </insert>

    <!-- 删除-->
    <delete id="delete" parameterType="String">
        delete from
          <include refid="tableName"></include>
        where
          user_id = #{userId}
    </delete>

    <!-- 通过 Username 查找 UserInfo -->
    <select id="findByUsername" resultType="UserInfo">
        SELECT
        *
        FROM
          <include refid="tableName"></include>
        WHERE username=#{username}
    </select>

    <update id="update" parameterType="UserInfo">
        UPDATE <include refid="tableName"></include>
        SET
        user_id=#{userId},
        username=#{username},
        password=#{password},
        phone=#{phone},
        sex=#{sex},
        attachment_id=#{attachmentId},
        birthday=#{birthday},
        ID_num=#{IDNum},
        real_name=#{realName},
        registration_date=#{registrationDate},
        school=#{school},
        shipping_address=#{shippingAddress},
        blood_type=#{bloodType},
        constellation=#{constellation}
        WHERE user_id=#{userId}
    </update>
</mapper>
